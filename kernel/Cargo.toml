[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "menix"
path = "main.rs"

[dependencies]
limine = "0.3.1"

[features]
default = ["smp", "boot_limine", "fw_acpi"]
smp = []
boot_limine = []
fw_acpi = []
fw_of = []
