#include <menix/log.h>
#include <menix/util/cmd.h>

void cmd_parse(CmdOptions* target, const char* cmd_line)
{
	kassert(target != NULL, "No target given!");
	kassert(cmd_line != NULL, "No target given!");
}
