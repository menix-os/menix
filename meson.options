option(
    'headers_only',
    type: 'boolean',
    value: false,
    description: 'Only install headers',
)

option(
    'boot_protocol',
    type: 'combo',
    choices: ['limine', 'uefi'],
    value: 'limine',
    description: 'The boot protocol to start the kernel with',
)
